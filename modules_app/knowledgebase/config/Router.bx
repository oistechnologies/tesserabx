/**
 * knowledgebase module router.
 *
 * The public read surface lives in the portal module (portal:Kb at
 * /kb, declared in the app-level config/Router.bx) so there is a
 * single source of truth that renders markdown bodies. This module
 * owns the Article / Category / feedback entities, the
 * KnowledgebaseService, the agent dashboard widgets, and the KB image
 * service; it intentionally declares no public routes of its own.
 */
class {

    function configure(){
    }

}
