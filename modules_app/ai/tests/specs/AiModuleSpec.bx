class extends="coldbox.system.testing.BaseTestCase" appMapping="/" {

    function beforeAll(){
        super.beforeAll();
        super.setup();
    }

    function run(){
        describe( "ai module", function(){
            it( "is registered with ColdBox", function(){
                var modules = getController().getModuleService().getModuleRegistry();
                expect( modules ).toHaveKey( "ai" );
            } );

            it( "is service-only (no HTTP entry point)", function(){
                // The AI layer is a service facade; Phase 5 surfaces
                // configuration through /agent/admin, not /ai.
                var cache = getController().getModuleService().getModuleConfigCache();
                expect( cache ).toHaveKey( "ai" );
                expect( cache[ "ai" ].entryPoint ).toBe( "" );
            } );
        } );
    }

}
