<bx:output>
<bx:set local.agent  = args.agent  ?: { hasAvatar : false } >
<bx:set local.size   = args.size   ?: 32 >
<bx:set local.prefix = args.prefix ?: "agents" >
#getInstance( "AvatarHelper@core" ).renderFromStruct( local.agent, local.size, local.prefix )#
</bx:output>
