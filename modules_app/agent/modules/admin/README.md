# admin

Administration and configuration surface, nested inside `agent`, resolving to `/agent/admin`. Loads as part of agent's lifecycle. Ships its own module-level cbSecurity rules gated to high-privilege roles.
