<bx:output>
<bx:set local.tiles = prc.tiles >
#view( view = "_partials/small_box", module = "core", args = {
    color     : "primary",
    metric    : encodeForHTML( local.tiles.openTickets ),
    label     : "Open tickets",
    icon      : "bi bi-card-list",
    link      : "/agent/tickets?status=open",
    linkLabel : "View open queue"
} )#
</bx:output>
