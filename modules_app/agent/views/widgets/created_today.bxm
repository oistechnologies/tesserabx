<bx:output>
<bx:set local.tiles = prc.tiles >
#view( view = "_partials/small_box", module = "core", args = {
    color   : "info",
    metric  : encodeForHTML( local.tiles.createdToday ),
    label   : "Created today",
    icon    : "bi bi-plus-circle"
} )#
</bx:output>
