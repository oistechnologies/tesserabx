<bx:output>
<!--- Persona dispatcher: signed-in Contact -> dashboard, anyone else -> guest. --->
<bx:if ( prc.showDashboard ?: false )>
    #view( "home/dashboard" )#
<bx:else>
    #view( "home/guest" )#
</bx:if>
</bx:output>
